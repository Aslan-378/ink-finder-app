<div class="container">
      <% @styles.each do |style| %>
      <div class="card-product pt-2">
      <%= link_to style do %>

      <% end %>
      <div class="card-product-infos">
      <h2><%= style.name %></h2>
      </div>
      </div>
    <% end %>
</div>
